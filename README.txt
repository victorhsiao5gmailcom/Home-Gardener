Please run this commend before running the APP:
    pip install google.generativeai

Run the __main__.py in cmd show the test result

Add images (.png) to plant_recognition_test_images folder and change the image_path to try different result